---
import { sanity } from "../lib/sanity"

type Service = {
  title: string
  subtitle?: string
  href: string
}

const services: Service[] = await sanity.fetch(`*[_type == "service"]|order(order asc){
  title,
  subtitle,
  href
}`)

const MP4 =
  "https://res.cloudinary.com/hardcodepunk/video/upload/q_auto:eco,vc_h264,ac_aac,f_mp4/v1737957147/wsuszohtmu2pks673muc.mp4"

const WEBM =
  "https://res.cloudinary.com/hardcodepunk/video/upload/q_auto:eco,vc_vp9,f_webm/v1761381373/b8f7chk3u9s6jaqh4bae.webm"

const POSTER =
  "https://res.cloudinary.com/hardcodepunk/video/upload/q_auto:eco,so_0,f_jpg,w_1600/v1737957147/wsuszohtmu2pks673muc.jpg"
---

<section class="grid w-full grid-cols-1 sm:grid-cols-2">
  {
    services.slice(0, 4).map((s, i) => (
      <a href={s.href} class="group relative block aspect-video w-full overflow-hidden">
        <div class="absolute inset-0 z-0">
          <video
            class="absolute left-0 top-1/2 h-[110%] w-full -translate-y-1/2 object-cover"
            muted
            playsinline
            autoplay
            loop
            preload={i === 0 ? "auto" : "metadata"}
            poster={POSTER}
            aria-hidden="true"
          >
            <source src={WEBM} type="video/webm" />
            <source src={MP4} type="video/mp4" />
          </video>
        </div>

        <div class="pointer-events-none absolute inset-0 z-[5] bg-black/50" />
        <div class="pointer-events-none absolute inset-0 z-10 bg-gradient-to-t from-black/55 via-black/15 to-transparent" />

        <div class="absolute inset-x-0 bottom-0 z-30 px-6 pb-6 text-left sm:px-8 sm:pb-8">
          <h2 class="font-display text-3xl uppercase text-ocean sm:text-4xl lg:text-5xl">{s.title}</h2>

          {s.subtitle ? <p class="pt-2 text-xs uppercase text-paper sm:text-sm">{s.subtitle}</p> : null}
        </div>
      </a>
    ))
  }
</section>
